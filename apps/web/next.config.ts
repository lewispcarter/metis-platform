/**
 * NEXT CONFIGURATION
 * Purpose: Configures the operational dashboard frontend application.
 * Role: Keeps web app configuration explicit and minimal for the initial foundation.
 */
import type { NextConfig } from 'next';

const nextConfig: NextConfig = {};

export default nextConfig;
